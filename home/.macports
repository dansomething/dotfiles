#!/bin/bash

# Make sure we're using the latest Macport
sudo port selfupdate

# Upgrade any already installed ports
sudo port upgrade outdated

# Install source controll tools with bash_completion
sudo port install subversion +bash_completion
sudo port install git-core +svn +bash_completion

# Make Macport's bash the default shell
chsh -s /opt/local/bin/bash
